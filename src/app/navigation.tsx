'use client';

// Navigation is now handled by the Header component in src/components/header.tsx
// This component is kept for compatibility but renders nothing.
export function Navigation() {
  return null;
}
